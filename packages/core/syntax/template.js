/* istanbul ignore file */

const { rule, bean, map, mapfn, ignore, merge } = require("bean-parser");
const { lPluck } = require("bean-parser");
const types = require("../../types");
const {
  topLevels,
  controls,
  expressions,
  arrayLike,
  wrap,
  ranges,
  values,
} = require("../common");

module.exports = {
  /* rules */
};
